<div class="container-fluid">
      <div class = "col-12 d-flex flex-row justify-content-center">
        <div class = "row">
          <h4 id="notice"><%= notice %></h4>
            <h4>Trip : <%= @plan.itinerary_name%></h4>
            <br />
            <br />
            <br />
          </div>
      </div>
      <div class = "col-12 d-flex justify-content-center">
        <div class="row">

              <%= button_to 'Get some inspiration', locations_path,{method:'get', class:'btn btn-primary'}%> &ensp; &ensp;
              <%= button_to 'Add Place', new_plan_place_path(@plan.id),{method:'get', class:'btn btn-primary'} %> &ensp; &ensp;
              <%= button_to 'View Plan', plans_places_path(@plan.id),{method:'get', class:'btn btn-primary'} %>

            </div>

        </div>


    </div>
